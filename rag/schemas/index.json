{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "IndexSchema",
  "description": "Schema for a collection of RAG chunks forming a repository index.",
  "type": "object",
  "properties": {
    "repository": {
      "type": "string",
      "description": "Name of the chunked repository."
    },
    "commit": {
      "type": "string",
      "description": "Commit SHA of the repository."
    },
    "generated_at": {
      "type": "string",
      "description": "ISO timestamp when the index was generated."
    },
    "chunks": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Unique identifier for the chunk."
          },
          "filepath": {
            "type": "string",
            "description": "Relative path of the source file."
          },
          "type": {
            "type": "string",
            "enum": ["class", "function", "module", "block", "text"],
            "description": "The structural type of the chunked content."
          },
          "name": {
            "type": "string",
            "description": "Optional name of the code element (e.g., class name, function name)."
          },
          "start_line": {
            "type": "integer",
            "minimum": 1,
            "description": "1-based starting line number in the source file."
          },
          "end_line": {
            "type": "integer",
            "minimum": 1,
            "description": "1-based ending line number in the source file."
          },
          "content": {
            "type": "string",
            "description": "The raw text or code content of the chunk."
          },
          "tokens": {
            "type": "integer",
            "minimum": 0,
            "description": "Estimated token count of the content."
          }
        },
        "required": ["id", "filepath", "type", "content"],
        "additionalProperties": false
      },
      "description": "List of chunked code elements or text documents."
    }
  },
  "required": ["repository", "commit", "chunks"],
  "additionalProperties": false
}
